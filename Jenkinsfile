pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/HugoCagninacciBerthier/Jenkins.git', 
                    branch: 'main', 
            }
        }
    }
}
